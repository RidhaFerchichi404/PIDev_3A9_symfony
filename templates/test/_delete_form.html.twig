<form method="post" action="{{ path('app_test_delete', {'AbonnementID': abonnement.AbonnementID}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ abonnement.AbonnementID) }}">
    <button class="btn">Delete</button>
</form>
