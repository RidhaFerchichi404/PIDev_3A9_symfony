{% extends 'base.html.twig' %}

{% block title %}New Order - Sportify Admin{% endblock %}

{% block body %}
    <div class="page-header">
        <div class="d-flex justify-content-between align-items-center">
            <div>
                <h1>Create New Order</h1>
                <p class="text-muted">Create a new order in the system</p>
            </div>
            <div>
                <a href="{{ path('app_admin_commande_index') }}" class="btn btn-outline">
                    <i class="fas fa-arrow-left me-2"></i>Back to List
                </a>
            </div>
        </div>
    </div>

    <div class="row justify-content-center">
        <div class="col-xl-8 col-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0">Order Information</h5>
                </div>
                <div class="card-body">
                    {{ include('P_Cadmin/commande/_form.html.twig', {'button_label': 'Create Order'}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %} 